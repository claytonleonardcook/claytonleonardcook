import type { Meta, StoryObj } from '@storybook/svelte';

import {{name}} from '@components/{{type}}s/{{name}}.svelte';

const meta = {
  title: '{{type}}s/{{name}}',
  component: {{name}},
  argTypes: {
    className: {
      table: {
        disable: true,
      },
    },
  },
  parameters: {
    controls: { 
      exclude: /^default*/,
      sort: 'alpha',
    },
  }
} satisfies Meta<{{name}}>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    _slot: 'Default',
  },
};